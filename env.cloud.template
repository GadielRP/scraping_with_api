# Cloud Environment Configuration for SofaScore Odds System
# Copy this to .env and fill in your actual values

# Database Configuration (defaults aligned with your current working .env)
# For Docker Compose (persists in ./data):
DATABASE_URL=sqlite:////app/data/sofascore_odds.db
# For direct server (uncomment this instead if not using Docker):
# DATABASE_URL=sqlite:////opt/sofascore/sofascore_odds.db
# For PostgreSQL (optional):
# DATABASE_URL=postgresql://username:password@localhost:5432/sofascore_odds

# Redis Configuration (optional, not used by default)
# REDIS_URL=redis://localhost:6379

# Proxy Configuration (if using Oxylabs)
PROXY_ENABLED=false
PROXY_USERNAME=your_proxy_username
PROXY_PASSWORD=your_proxy_password
PROXY_ENDPOINT=your_proxy_endpoint

# Scheduler Configuration (kept from your working .env)
POLL_INTERVAL_MINUTES=5
DISCOVERY_INTERVAL_HOURS=2
PRE_START_WINDOW_MINUTES=30

# Timezone (kept from your working .env)
TIMEZONE=America/Mexico_City

# Logging (kept from your working .env)
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Rate Limiting (kept from your working .env)
REQUEST_DELAY_SECONDS=1.0
MAX_RETRIES=3

# Notifications - Telegram (filled from your working .env)
NOTIFICATIONS_ENABLED=true
TELEGRAM_BOT_TOKEN=7448644393:AAFpnV4HgL4eZ17uHTFh3DejdvzMXFah7FE
TELEGRAM_CHAT_ID=-4615347478

# Proxy Rotation
PROXY_ROTATION_INTERVAL=5
PROXY_MAX_RETRIES=3

# Proxy Configuration (filled from your working .env)
PROXY_ENABLED=true
PROXY_USERNAME=sofascore_dMkbW
PROXY_PASSWORD=+Sofascoremanga10
PROXY_ENDPOINT=pr.oxylabs.io:7777
PROXY_ROTATION_INTERVAL=5
PROXY_MAX_RETRIES=3
